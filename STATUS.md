# STATUS — multi-terminal heartbeat

Append a line after every meaningful action (commit, decision, blocker, hand-off).
Read the tail of this file at the start of every turn to see what other
terminals are doing.

## Protocol

- Format: `- HH:MM [tag] action — files or commit short-sha`
- `tag` is whatever identifies this terminal: A / B / C, or a topic
  ("luma", "evermind", "scaffold"). Pick at start of session, stick with it.
- **Append-only.** Don't edit past lines.
- **Push immediately** after appending so others see it.
- Heartbeat at least every 15 min even if just "still on X".

## Log

- 13:34 [A] Init: README + first commit `e1bb56d`, GitHub repo created (`jennyruan/socialbutter`, public).
- 13:43 [A] CLAUDE.md (project rules) + Luma fetcher + drafts/ (UI, CSS, route) pushed `a963508`.
- 13:43 [B] Added `.mcp.json` (Butterbase MCP) + `scripts/evermind-smoke.mjs` (API validation).
- 13:52 [A] Pivoted Luma fetcher to URL-paste flow (profile API is private); organizer-array fix `736b617`.
- 14:50 [A] Agent (rank + draft + feedback) + LLM client pushed `93b5f4f`. Awaiting scaffold + `lib/evermind.ts`.
- 15:05 [A] Added STATUS.md + sync-hook for near-real-time coordination.
- 15:18 [A] Luma "connect" via personal iCal subscription URL — `fetchLumaEventsFromIcsUrl` + ICS parser in `lib/luma.ts`; `fetchLumaFromInput` routes ICS-vs-URL-paste; connect UI copy leads with calendar URL.
- 15:25 [A-opus] Verified Evermind LIVE — `scripts/evermind-smoke.mjs` write=202 search=200. Wrote `lib/evermind.ts` (HttpEvermindClient) implementing `EvermindClient`. ⚠️ Writes are async — pre-seed memories minutes before demo, don't seed live.
- 15:37 [A-opus] Scaffolded Next.js 15 + React 19 by hand. App is LIVE on http://localhost:3000/connect — `/api/luma/import` returns real today's-hackathon-event JSON (verified). Added .gitignore for `.next/`, `node_modules/`, `.env*`, `/.claude/settings.json`, `/seed/feedback.json`.
- 15:50 [A] Backstage browser agent shipped. `lib/browser-agent.ts` (Playwright, persistent Chrome profile @ `~/.socialbutter-browser-profile`, humanized delays/typing/scroll, webdriver-flag override, rate-limits LinkedIn=8/hr 25/day). Actions: `rsvpLumaEvent`, `linkedinConnect`, `runBackstageQueue`. One-time login via `node scripts/browser-agent-setup.mjs`. CLI testing via `pnpm exec tsx scripts/browser-agent-run.ts ...`. Added `playwright` + `tsx` to deps.
- 14:55 [A] **Project rename: ButterSocial → SocialButter.** Repo renamed on GitHub (jennyruan/buttersocial → jennyruan/socialbutter), git remote updated. Bulk sed across all tracked + untracked files: `ButterSocial`→`SocialButter`, `buttersocial`→`socialbutter`, `bs-`→`sb-` (CSS class prefix). `drafts/bs-tokens.css` → `drafts/sb-tokens.css`. Browser profile dir is now `~/.socialbutter-browser-profile`. Local dir path unchanged (still `~/code/experiments/buttersocial/`) for terminal-coordination safety; rename later.
- 15:02 [cal] Apple + Google Calendar connections shipped. New: `lib/ics.ts` (generic RFC 5545 parser, `webcal://` rewrite), `lib/calendar.ts` (multi-source fetcher: Luma / iCloud / Google / generic ICS, dedupe, conflict helpers), `app/api/calendar/import/route.ts` (accepts mixed input — subscription URLs + Luma event URLs in one paste). `app/connect/page.tsx` rewritten with 3-source help list + per-event source chip. Smoke-test on Google holiday calendar returned real events. `lib/agent.ts` `rankEvents()` now takes `busyEvents` and emits hard time-conflict citations ("Skip — conflicts with X on Google Calendar") — demo wow upgrade. NOTE: did NOT commit untracked `app/api/find-events/`, `app/api/find-people/`, `lib/social-search.ts` (other terminal's WIP).
- 16:12 [A-opus] Shipped X + LinkedIn search/lookup on top of [A]'s browser-agent → `lib/social-search.ts` (findEventsOnX/LinkedIn + findPersonOnX/LinkedIn, anti-detection via shared `getBrowserContext()`, typed `SocialSearchError` with cause). `/api/find-events` + `/api/find-people` routes (401 on not_logged_in, no mocks). `/connect` page restructured into 3 sections: Connect Luma · Find events · Look up person; SourceToggle (X/LinkedIn) + PersonCard with avatar/headline/bio/followers/recent posts. CSS for `.sb-toggle*`, `.sb-source-pill`, `.sb-person-*`. Commit `0a55c61`. Verified: find-events returns honest 401 not_logged_in; activation = `node scripts/browser-agent-setup.mjs` then sign in once.
- 15:08 [A] Butterfly logo (amber-fill, ink-stroke, painted style — matches Jenny's reference). Inline `<ButterflyLogo />` component in `app/connect/page.tsx` header; `public/butterfly.svg` for any image refs; `app/icon.svg` for Next favicon. Header `.sb-header-left` aligned center to accommodate the 44px logo. Already on main (merged through cb6782e).
- 15:05 [deploy] Replaced static landing at https://jenny-sandbox.butterbase.dev with demo-mode build. Single self-contained `index.html` (28KB, deployment_id `dae4f9af...`, framework=static). Hero now has "Try the demo →" CTA; new demo section between hero and "How it works" has sample profile (solo SF AI infra founder), 4 Evermind memory chips, 8 ranked events (3 GO / 2 MAYBE / 3 SKIP) with verdict badges + match scores + plain-language reasons + citation chips. Visual language matches local app (AMGINA tokens, butter SVG primitives, JetBrains Mono + LXGW WenKai). ⚠️ **Trade-off vs CLAUDE.md Rule #2 (no mock data):** events + memory chips are fabricated for public anon visitors who don't have Jenny's Luma. Hackathon judges' demo still uses Jenny's real Luma + real Evermind via the local app — this static demo is the *public marketing/preview* surface, not the live pitch flow. Source in `/tmp/sb-deploy/index.html` (not committed to repo — staged build). To redeploy: re-zip `/tmp/sb-deploy/`, `create_frontend_deployment(framework=static)`, PUT to uploadUrl, `manage_frontend(start_deployment)`.
- 15:14 [A] **One-click "Connect Luma/Google/Apple"** wired end-to-end. `lib/browser-agent.ts` adds `extractLumaSubscriptionUrl` (lu.ma/settings/calendar), `extractGoogleCalendarSubscriptionUrl(calendarName?)` (calendar.google.com settings, scrolls to find Secret iCal address), `extractAppleCalendarSubscriptionUrl(calendarName?)` (icloud.com — requires calendar already shared publicly, agent doesn't toggle). `app/api/calendar/auto-connect` POST `{source}` → extractor → `fetchCalendarFromUrl` → events. Connect page has 3 secondary buttons above manual paste field; success merges into the live event list and shows the URL. 401 if not signed in (run `node scripts/browser-agent-setup.mjs` once). Typecheck clean.
- 15:12 [deploy] Reverted mock demo per Jenny direction ("I will use my own accounts and data to test, no mockup"). Redeployed clean landing — same SocialButter rename, butter SVG art, hero email signup, "How it works" 3-step, but **no mock event cards or fake memory chips**. Footer now links to `github.com/jennyruan/socialbutter`. 15KB, deployment_id `6cdbc784...`. Public URL unchanged: https://jenny-sandbox.butterbase.dev. Real demo runs on localhost with Jenny's own Luma/Evermind during the pitch.
- 16:35 [A-opus] Full agent loop wired: `/api/rank` (Evermind + LLM rank for ANY events — Luma/X/LinkedIn/Google/Apple, unified via new `RankableEvent` interface in lib/agent.ts), `/api/draft-intro` (host-host OR person-person via new `draftPersonIntro` that takes recent posts + event context), `/api/find-attendees` (scrape Luma guest list via `findLumaEventAttendees` + optional rank via new `rankPeople`). UI: "Rank with Evermind" bar with optional goal input on every events list + GO/SKIP/MAYBE verdict overlay with cited memory pills. PersonCard now has "Draft intro" → editable agent draft. LumaEventCard has "Find attendees" → ranked guest list with per-person verdict pills. Compiles green (629 modules), `/connect` 200. Routes return honest errors (LLM_API_KEY not set; Chrome profile singleton conflict pre-setup) — no mocks.
