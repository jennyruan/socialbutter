# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# next.js
/.next/
/out/
*.tsbuildinfo
next-env.d.ts

# build / test
/build
/coverage

# secrets — never commit
.env*
!.env.example

# os / editor
.DS_Store
*.pem
.vscode
.idea

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Claude local overrides (project-level hook config is per-machine)
/.claude/settings.json
/.claude/settings.local.json

# real-data fixtures (gitignored to keep secrets/PII out)
/seed/feedback.json
